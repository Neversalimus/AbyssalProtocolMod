namespace AbyssalProtocol
{
    // Kept intentionally as a no-op compatibility stub.
    // Monster info-card icon sizing is now handled through XML uiIconScale values to avoid
    // adding a startup type-discovery dependency during RimWorld mod loading.
    public static class ABY_MonsterInfoCardIconNormalizer
    {
    }
}
