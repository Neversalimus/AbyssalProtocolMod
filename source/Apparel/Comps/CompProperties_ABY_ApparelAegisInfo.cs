using Verse;

namespace AbyssalProtocol
{
    public class CompProperties_ABY_ApparelAegisInfo : CompProperties
    {
        public CompProperties_ABY_ApparelAegisInfo()
        {
            compClass = typeof(CompABY_ApparelAegisInfo);
        }
    }
}
